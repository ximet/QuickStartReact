import { createSelector } from 'reselect';

export default createSelector(
    [
        
    ],
    (
        
    ) => {
        return {
            TITLE: 'WeatherOutputView',
        }
    },
);
